<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.genai.mapper.ParentMapper">
    <select id="countByEmail" parameterType="string" resultType="int">
        SELECT COUNT(*)
        FROM PARENT
        WHERE LOWER(email) = LOWER(#{email})
    </select>

    <select id="findByEmail" parameterType="string" resultType="Parent">
        SELECT parent_id, email, password_hash, name, created_at
        FROM PARENT
        WHERE LOWER(email) = LOWER(#{email})
    </select>

    <insert id="insert" parameterType="Parent">
        INSERT INTO PARENT (
            email,
            password_hash,
            name
        ) VALUES (
            #{email},
            #{passwordHash},
            #{name}
        )
    </insert>
</mapper>
